{{/*
All mock targets use the same testapp image in "mock" role.
All serve TLS/mTLS — certs are mounted from the mock-certs secret.
Envoy's outbound clusters connect to them over mTLS.
*/}}

{{- define "mock.tlsEnv" -}}
- name: LISTEN_ADDR
  value: "0.0.0.0"
- name: TLS_CERT
  value: {{ $.Values.mocks.tls.mountPath }}/tls.crt
- name: TLS_KEY
  value: {{ $.Values.mocks.tls.mountPath }}/tls.key
- name: TLS_CA
  value: {{ $.Values.mocks.tls.mountPath }}/ca.crt
{{- end }}

{{- define "mock.tlsVolumeMount" -}}
- name: mock-certs
  mountPath: {{ $.Values.mocks.tls.mountPath }}
  readOnly: true
{{- end }}

{{- define "mock.tlsVolume" -}}
- name: mock-certs
  secret:
    secretName: {{ $.Values.mocks.tls.secretName }}
{{- end }}

{{- define "mock.securityContext" -}}
securityContext:
  runAsNonRoot: true
  {{- if .Values.global.appUID }}
  runAsUser: {{ .Values.global.appUID }}
  {{- end }}
  allowPrivilegeEscalation: false
  capabilities:
    drop: ["ALL"]
{{- end }}

# ── Kafka mock (TCP echo over TLS) ────────────────────────────────────────────
{{- if .Values.mocks.kafka.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kafka-mock
  namespace: {{ .Values.namespaces.kafka }}
  labels:
    app: kafka-mock
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kafka-mock
  template:
    metadata:
      labels:
        app: kafka-mock
    spec:
      containers:
        - name: mock
          image: {{ .Values.global.imageRegistry }}{{ .Values.mocks.image }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          env:
            - name: APP_ROLE
              value: "mock"
            - name: APP_PORT
              value: "8080"
            - name: TCP_PORT
              value: {{ .Values.mocks.kafka.tcpPort | quote }}
            {{- include "mock.tlsEnv" . | nindent 12 }}
          ports:
            - containerPort: 8080
              name: https
            - containerPort: {{ .Values.mocks.kafka.tcpPort }}
              name: tcp-tls
          volumeMounts:
            {{- include "mock.tlsVolumeMount" . | nindent 12 }}
          readinessProbe:
            # tcpSocket, not httpGet: the mock serves mTLS and kubelet has no
            # client cert, so an HTTPS probe fails the handshake. A TCP check
            # confirms the listener is up, which is enough for a mock.
            tcpSocket:
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 5
          {{ include "mock.securityContext" . | nindent 10 }}
      volumes:
        {{- include "mock.tlsVolume" . | nindent 8 }}
---
{{- end }}

# ── LLM Gateway mock ──────────────────────────────────────────────────────────
{{- if .Values.mocks.llmGateway.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: llm-gateway-mock
  namespace: {{ .Values.namespaces.llmGateway }}
  labels:
    app: llm-gateway-mock
spec:
  replicas: 1
  selector:
    matchLabels:
      app: llm-gateway-mock
  template:
    metadata:
      labels:
        app: llm-gateway-mock
    spec:
      containers:
        - name: mock
          image: {{ .Values.global.imageRegistry }}{{ .Values.mocks.image }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          env:
            - name: APP_ROLE
              value: "mock"
            - name: APP_PORT
              value: {{ .Values.mocks.llmGateway.httpPort | quote }}
            {{- include "mock.tlsEnv" . | nindent 12 }}
          ports:
            - containerPort: {{ .Values.mocks.llmGateway.httpPort }}
              name: https
          volumeMounts:
            {{- include "mock.tlsVolumeMount" . | nindent 12 }}
          readinessProbe:
            tcpSocket:
              port: {{ .Values.mocks.llmGateway.httpPort }}
            initialDelaySeconds: 3
            periodSeconds: 5
          {{ include "mock.securityContext" . | nindent 10 }}
      volumes:
        {{- include "mock.tlsVolume" . | nindent 8 }}
---
{{- end }}

# ── Internal API mock ─────────────────────────────────────────────────────────
{{- if .Values.mocks.internalAPI.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: internal-api-mock
  namespace: {{ .Values.namespaces.internalApi }}
  labels:
    app: internal-api-mock
spec:
  replicas: 1
  selector:
    matchLabels:
      app: internal-api-mock
  template:
    metadata:
      labels:
        app: internal-api-mock
    spec:
      containers:
        - name: mock
          image: {{ .Values.global.imageRegistry }}{{ .Values.mocks.image }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          env:
            - name: APP_ROLE
              value: "mock"
            - name: APP_PORT
              value: {{ .Values.mocks.internalAPI.httpPort | quote }}
            {{- include "mock.tlsEnv" . | nindent 12 }}
          ports:
            - containerPort: {{ .Values.mocks.internalAPI.httpPort }}
              name: https
          volumeMounts:
            {{- include "mock.tlsVolumeMount" . | nindent 12 }}
          readinessProbe:
            tcpSocket:
              port: {{ .Values.mocks.internalAPI.httpPort }}
            initialDelaySeconds: 3
            periodSeconds: 5
          {{ include "mock.securityContext" . | nindent 10 }}
      volumes:
        {{- include "mock.tlsVolume" . | nindent 8 }}
---
{{- end }}

# ── Blocked mock ──────────────────────────────────────────────────────────────
{{- if .Values.mocks.blocked.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: blocked-mock
  namespace: {{ .Values.namespaces.blocked }}
  labels:
    app: blocked-mock
spec:
  replicas: 1
  selector:
    matchLabels:
      app: blocked-mock
  template:
    metadata:
      labels:
        app: blocked-mock
    spec:
      containers:
        - name: mock
          image: {{ .Values.global.imageRegistry }}{{ .Values.mocks.image }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          env:
            - name: APP_ROLE
              value: "mock"
            - name: APP_PORT
              value: {{ .Values.mocks.blocked.httpPort | quote }}
            {{- include "mock.tlsEnv" . | nindent 12 }}
          ports:
            - containerPort: {{ .Values.mocks.blocked.httpPort }}
              name: https
          volumeMounts:
            {{- include "mock.tlsVolumeMount" . | nindent 12 }}
          readinessProbe:
            tcpSocket:
              port: {{ .Values.mocks.blocked.httpPort }}
            initialDelaySeconds: 3
            periodSeconds: 5
          {{ include "mock.securityContext" . | nindent 10 }}
      volumes:
        {{- include "mock.tlsVolume" . | nindent 8 }}
{{- end }}
