# Normalise line endings to LF in the repo; checkout as-is on all platforms.
* text=auto eol=lf

# Shell scripts must always be LF (they run on Linux in containers)
*.sh text eol=lf

# Windows-specific files can keep CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
